name = "museStarlinkpay71"
main = "worker.js"
# 不要使用顶层 "type" 字段（这个字段在某些 wrangler/配置下会触发错误）
account_id = "APU470Hi_e3CmNFbSLr-gREKxjwiggaLf8cwPk_5"
workers_dev = true
compatibility_date = "2025-11-03"

[triggers]
crons = ["0 * * * *"] # 每小时触发一次

# 正确的 kv_namespaces 格式：一个数组（不是包含 bindings 的对象）
kv_namespaces = [
  { binding = "NODES_KV", id = "ff5529a841224bb6b36788cbc51bdafa" },
  { binding = "RATE_KV", id = "ff5529a841224bb6b36788cbc51bdafa" }
]

[vars]
# 如果你不想把默认源写死在文件里，建议把 DEFAULT_SOURCES 通过 secrets 注入或在这里保留空 '[]'
DEFAULT_SOURCES = '[]'

# 备注：自定义域路由需在 Cloudflare Dashboard 手工添加 (pay71.snk567.cc/cc -> 你的 Worker)
